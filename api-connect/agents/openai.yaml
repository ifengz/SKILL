interface:
  display_name: "API Connect"
  short_description: "设计同源代理、三方协议路由、密钥传输和错误归一化合同"
  default_prompt: "Use $api-connect to turn this provider proxy idea into an implementation-ready transport contract."
policy:
  allow_implicit_invocation: true
